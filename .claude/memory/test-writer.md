# test-writer Agent Memory

## User Preferences
- Building a multi-tenant restaurant financial dashboard — everything must be generic, no hardcoded data
- Prefers clean, compact UIs — no visual noise
- Values data accuracy above all — verify numbers match before shipping
- Dark mode is primary theme
- Never fail silently — always surface errors
- No hardcoded restaurant names, menu items, or categories

## Patterns Discovered
(will be populated as this agent runs)

## Common Mistakes Found
(will be populated as this agent runs)

## Lessons Learned
(will be populated as this agent runs)
